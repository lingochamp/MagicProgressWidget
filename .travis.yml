language: android

android:
  components:
    - platform-tools
    - build-tools-27.0.3
    - android-23
    - extra

after_script:
    - cat ./demo/build/outputs/lint-results.xml
    - cat ./library/build/outputs/lint-results.xml

sudo: false